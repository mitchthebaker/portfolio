version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install 
  build:
    commands:
      - npm react-scripts build 

artifacts:
  files:
    - '**/*'
  base-directory: 'build'